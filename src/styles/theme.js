const theme = {
    colors:{
        primary: '#3f51b5',
        background: '#f5f5f5',
        text: '#222',
        card: '#fff'
    },
    spacing: (factor) => `${factor * 8}px`
}

export default theme;
